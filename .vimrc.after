set background=dark
color solarized
set guifont=Inconsolata:h14

" Always us relative line numbers
set relativenumber
au BufRead,BufNewFile,BufWinEnter * set relativenumber

" Integrate with OS X clipboard
set clipboard=unnamed

if has("gui_running")
  set guioptions=-t
endif
